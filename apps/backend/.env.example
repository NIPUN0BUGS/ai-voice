FRONTEND_ORIGIN=http://localhost:5173
AUDIO_STORAGE_PATH=/tmp/voice-audio
PORT=8000

# Use mock for the base backend install. For real transcription, install
# requirements-asr.txt and set ASR_PROVIDER=faster-whisper.
ASR_PROVIDER=mock
ASR_MODEL_SIZE=tiny
ASR_DEVICE=cpu
ASR_COMPUTE_TYPE=int8
